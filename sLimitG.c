#include "mySAT.h"#include "ascent.h"/*Globals*/extern Globals			g;extern PixMapHandle		limitsOffScreenBuffer;						//Declared on game.cextern SpritePtr		topLeftLG, topRightLG;					//Declared on misc.cextern Rect				leftLimitBkg, leftLimitR, rightLimitBkg, rightLimitR;/*Code*/pascal void SetupLimitGenerator(SpritePtr generator){	generator->task = &HandleLimitGenerator;	generator->hitTask = &HitTaskLimitGenerator;	generator->face = SATGetFace(kLimitGID);	generator->layer = 0;	generator->mode = kLimitGAliveMode;	generator->shields = kLimitGShields;	SetRect(&generator->hotRect, 0, 0, 20, 29);}	pascal void HandleLimitGenerator(SpritePtr generator){SATPort		oldPort;int			expCoord;	switch(generator->mode)	{	case kLimitGAliveMode:		if(generator->shields < 0)		{			Explode(generator, 25, 15, 10, 5, g.explosionSnd);			generator->face = SATGetFace(kLimitGDeadID);			generator->mode = kLimitGDestroyedMode;						if(generator->position.h < gSAT.offSizeH/2)				//If this is the limit on the left half of the screen			{				//Copy the apropiate strip of background to erease the no longer present green beam				CopyBits((BitMap*)*limitsOffScreenBuffer, &gSAT.backScreen.port->portBits, &leftLimitBkg, &leftLimitR, srcCopy, nil);				Explode(topLeftLG, 15, 5, 7, 3, g.explosionSnd);	//Also make an explosion at the apropiate reciever and change its graphic								topLeftLG->face = SATGetFace(kLimitRecieverOffID);			}else			{				CopyBits((BitMap*)*limitsOffScreenBuffer, &gSAT.backScreen.port->portBits, &rightLimitBkg, &rightLimitR, srcCopy, nil);				Explode(topRightLG, 15, 5, 7, 3, g.explosionSnd);				topRightLG->face = SATGetFace(kLimitRecieverOffID);			}			SATBackChanged(&gSAT.wind.bounds);				}			break;					case kLimitGDestroyedMode:								//Randomly and sparsely create sparks		if(!SATRand(60))			Sparkle(generator, SATRand(5)+1, 1);		break;			}		}pascal void HitTaskLimitGenerator(SpritePtr me, SpritePtr him){	if(him->kind == kBulletKind)	{		me->shields -= kBulletDamage;	}	}pascal void SetupLimitReciever(SpritePtr reciever){	reciever->task = &HandleLimitReciever;	reciever->face = SATGetFace(kLimitRecieverOnID);	reciever->layer = 0;	SetRect(&reciever->hotRect, 0, 0, 23, 10);}pascal void HandleLimitReciever(SpritePtr reciever){	}
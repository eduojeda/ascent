#include "mySAT.h"#include "ascent.h"/*Globals*/extern Globals		g;/*Code*/pascal void SetupSmoke(SpritePtr smoke){	smoke->task = &HandleSmoke;	smoke->layer = kSmokeLayer;	smoke->counter = 0;	smoke->speed.h = 0;	smoke->speed.v = 0;	SetRect(&smoke->hotRect, 0,0,0,0);}pascal void HandleSmoke(SpritePtr smoke){	smoke->face = g.smokeFaces[smoke->counter/3];								smoke->counter++;																if(smoke->counter == 18)	{		smoke->task = nil;	}		smoke->position.h += smoke->speed.h;	smoke->position.v += smoke->speed.v;}	
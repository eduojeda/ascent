#include "mySAT.h"#include "ascent.h"/*Globals*/extern Globals		g;/*Code*/pascal void SetupSpark(SpritePtr spark){	spark->task = &HandleSpark;	spark->layer = kSparkLayer;	spark->counter = 0;	spark->speed.h = 0;	spark->speed.v = 0;	SetRect(&spark->hotRect, 0,0,0,0);}	pascal void HandleSpark(SpritePtr spark){	spark->face = g.sparkFaces[spark->counter/4];								spark->counter++;																if(spark->counter == 20)	{		spark->task = nil;	}			spark->position.h += spark->speed.h;	spark->position.v += spark->speed.v;}	pascal void SetupSparkler(SpritePtr sparkler){	sparkler->task = &HandleSparkler;	SetRect(&sparkler->hotRect, 0,0,0,0);}pascal void HandleSparkler(SpritePtr sparkler){static int i = 0, j = 0;		if(j <= 0)		j = sparkler->counter;			if(j <= 1)	{		sparkler->task = nil; 		j--;	}j--;			if(i >= sparkler->counter/j)	{		Sparkle(sparkler->who, 1, 0);		i = 0;	}i++;}void Sparkle(SpritePtr object, short numSparks, short sparkSpeed){SpritePtr	spark;int 		i;	for(i = 0 ; i <= numSparks ; i++)										//Creates numSparks sparks	{			spark = SATNewSprite(kSparkKind, 							 object->position.h + object->hotRect.right/2 + (SATRand(object->hotRect.right-5)-SATRand(object->hotRect.right-5)),							 object->position.v + object->hotRect.bottom/2 + (SATRand(object->hotRect.bottom-5)-SATRand(object->hotRect.bottom-5)),							 &SetupSpark);		if(sparkSpeed != 0)		{			spark->speed.h = SATRand(sparkSpeed)-SATRand(sparkSpeed);		//Gives the spark a random direction of movement			spark->speed.v = SATRand(sparkSpeed)-SATRand(sparkSpeed);		}			}	}